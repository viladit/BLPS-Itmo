spring.datasource.url=${DB_URL:jdbc:postgresql://127.0.0.1:${DB_PORT:5434}/studs}
spring.datasource.username=${DB_USERNAME:s368925}
spring.datasource.password=${DB_PASSWORD:5mCSFOGOnfrbfJeC}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.transaction.coordinator_class=jta
spring.jpa.properties.hibernate.transaction.jta.platform=org.hibernate.engine.transaction.jta.platform.internal.JBossStandAloneJtaPlatform
spring.jpa.open-in-view=false
spring.sql.init.mode=never

camunda.bpm.enabled=true
camunda.bpm.database.schema-update=true
camunda.bpm.authorization.enabled=true
camunda.bpm.filter.create=All tasks
camunda.bpm.deployment-resource-pattern=classpath*:**/*.bpmn
camunda.bpm.job-execution.enabled=true
camunda.bpm.generic-properties.properties.skipIsolationLevelCheck=true

app.camunda.identity-seed.enabled=${APP_CAMUNDA_IDENTITY_SEED_ENABLED:true}
app.camunda.skip-isolation-level-check=${APP_CAMUNDA_SKIP_ISOLATION_LEVEL_CHECK:true}

app.tx.vendor=postgresql
app.tx.object-store-dir=${NARAYANA_OBJECT_STORE_DIR:target/narayana-object-store}
app.tx.orders.unique-name=orders-db
app.tx.orders.url=${DB_URL:jdbc:postgresql://127.0.0.1:${DB_PORT:5434}/studs}
app.tx.orders.username=${DB_USERNAME:s368925}
app.tx.orders.password=${DB_PASSWORD:5mCSFOGOnfrbfJeC}
app.tx.notifications.unique-name=notifications-db
app.tx.notifications.url=${NOTIFICATION_DB_URL:jdbc:postgresql://127.0.0.1:${NOTIFICATION_DB_PORT:5435}/notifications}
app.tx.notifications.username=${NOTIFICATION_DB_USERNAME:notifications}
app.tx.notifications.password=${NOTIFICATION_DB_PASSWORD:notifications}

app.security.seed.enabled=${APP_SECURITY_SEED_ENABLED:false}
app.security.seed.manager-password=${APP_SECURITY_MANAGER_PASSWORD:manager123}
app.security.seed.warehouse-password=${APP_SECURITY_WAREHOUSE_PASSWORD:warehouse123}
app.security.seed.delivery-password=${APP_SECURITY_DELIVERY_PASSWORD:delivery123}
app.security.seed.admin-password=${APP_SECURITY_ADMIN_PASSWORD:admin123}

app.notifications.jms.enabled=${APP_NOTIFICATIONS_JMS_ENABLED:false}
app.jms.host=${APP_JMS_HOST:localhost}
app.jms.port=${APP_JMS_PORT:5672}
app.jms.username=${APP_JMS_USERNAME:guest}
app.jms.password=${APP_JMS_PASSWORD:guest}
app.jms.queue-name=${APP_JMS_QUEUE_NAME:order.status.queue}

app.orders.pending-reminder.enabled=${APP_PENDING_REMINDER_ENABLED:false}
app.orders.pending-reminder.age=${APP_PENDING_REMINDER_AGE:PT10M}
app.orders.pending-reminder.initial-delay=${APP_PENDING_REMINDER_INITIAL_DELAY:PT30S}
app.orders.pending-reminder.fixed-delay=${APP_PENDING_REMINDER_FIXED_DELAY:PT1M}
app.orders.pending-reminder.repeat-interval=${APP_PENDING_REMINDER_REPEAT_INTERVAL:PT15M}

app.eis.bitrix.enabled=${APP_EIS_BITRIX_ENABLED:false}
app.eis.bitrix.webhook-url=${APP_EIS_BITRIX_WEBHOOK_URL:}
app.eis.bitrix.responsible-id=${APP_EIS_BITRIX_RESPONSIBLE_ID:1}
